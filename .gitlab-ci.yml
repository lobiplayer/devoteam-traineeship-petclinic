stages:
  - build
  - test
  - deploy

build:
  stage: build
  tags:
    - cipipeline
  script:
    - mvn compile

test:
  stage: test
  tags:
    - cipipeline
  script:
    - mvn clean test

deploy:
  stage: deploy
  tags:
    - cipipeline
  script:
    - mvn clean deploy -P release
