stages:
  - build
  - test
  - deploy

build:
  stage: build
  tags:
    - cipipeline
  script:
    - mvn compile

test:
  stage: test
  tags:
    - cipipeline
  script:
    - mvn clean test

deploy:
  stage: deploy
  tags:
    - cipipeline
  script:
    - mvn jetty:run-war -Djetty.port=9999
