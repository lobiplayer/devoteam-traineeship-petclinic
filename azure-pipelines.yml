# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

#why is this not working?!!
trigger:
- azurerunner

pool:
  name: 'petclinicpool'
  demands:
  - maven

stages:
- stage: Build
  jobs:
  - job: maven_build
    steps:
    - script: mvn compile
- stage: Test
  jobs:
  - job: maven_test
    steps:
    - script: mvn clean test
- stage: Deploy
  jobs:
  - job: maven_deploy
    steps:
    - script: mvn  -Drepo.id=nexusdeploymentrepo -Drepo.login=admin -Drepo.pwd=AK9wSQXi9xdvj6B clean deploy
